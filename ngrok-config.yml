version: "2"
authtoken: YOUR_NGROK_AUTH_TOKEN_HERE

tunnels:
  inflow-webhook:
    proto: http
    addr: 8000
    bind_tls: true
    # Optional: Set a custom subdomain (requires paid ngrok plan)
    # subdomain: inflow-error-gate

